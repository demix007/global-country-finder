import React from 'react';
import Countries from '../Countries/Countries';
import Filter from '../Filter/Filter';

const Body = () => (
  <div>
    <Filter />
    <Countries />
  </div>
);

export default Body;
